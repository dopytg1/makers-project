'use strict';

window.addEventListener("resize", function() {
    this.clearInterval
    let width = window.innerWidth;
    let height = window.innerHeight;
    
    this.console.log(`width: ` + width);
    this.console.log(`height: ` + height);
    this.console.log(`sum: ` + (width + height));
});


